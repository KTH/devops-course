name: 'Verify KTH GitHub id'
description: 'Greet someone and record the time'
inputs:
  token:
    description: >
      GitHub token for GitHub API requests.
      Users shouldn't need to set this themselves.
    required: true
    default: ${{ github.token }}
runs:
  using: 'node12'
  main: 'dist/index.js'